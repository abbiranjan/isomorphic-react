const { describe } = require("optimist");

import NotificationsViewer from '../NotificationsViewer';
import React from 'react';
import delay from 'redux-saga';

describe("Notifications Viewer", () => {
    it ("should display correct number of notifications", async() => {

    });
});